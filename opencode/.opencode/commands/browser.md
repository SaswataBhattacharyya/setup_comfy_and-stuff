---
description: Inspect or debug the local UI using the browser agent and Playwright MCP.
agent: browser
---

Use Playwright MCP to inspect or debug the local UI.

Task:
$ARGUMENTS

Ask before starting the dev server.
Prefer accessibility-based inspection.
Summarize findings and suggest minimal fixes.
