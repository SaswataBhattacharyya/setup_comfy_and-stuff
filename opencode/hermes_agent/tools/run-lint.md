# Tool Workflow: run-lint

Purpose: run lint checks when configured.

Preferred commands:

```bash
npm run lint
```

or:

```bash
bun run lint
```

If no lint script exists, record that lint was unavailable instead of inventing one.
