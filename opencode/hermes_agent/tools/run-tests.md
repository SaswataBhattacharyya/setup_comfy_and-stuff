# Tool Workflow: run-tests

Purpose: run tests when configured.

Preferred commands:

```bash
npm run test
```

```bash
npm test
```

```bash
pytest
```

```bash
python -m pytest
```

Run targeted tests before full suites when possible. Record skipped tests and why.
