# Tool Workflow: run-build

Purpose: run the configured build command.

Preferred commands:

```bash
npm run build
```

or, if Bun is the project runtime:

```bash
bun run build
```

For this repo's frontend, run from `ai-art-generator-hub` when checking frontend build.

Record:

- command
- working directory
- pass/fail
- first actionable error
