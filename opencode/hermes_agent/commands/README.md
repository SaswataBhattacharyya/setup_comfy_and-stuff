# Hermes Commands

These are Hermes equivalents of the OpenCode command files. They are promptable workflows for Hermes CLI and should be copied with the pack.

Use them by asking Hermes to run the named command workflow, for example:

```text
Use command safe-ui-change: <task>
```

The commands are documentation-backed because Hermes CLI support for custom slash commands may differ by version.
